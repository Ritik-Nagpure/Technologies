# EXTRACTING 
result <- substring("Extract",5,7)
print(result)

if(FALSE){"the output is according to the numbering from a to b
  i.e herre 5-7 the alphabets on the index 5 to 7 are printed"}

print (seq(5,9,by=0.4))

#sorting

v <-  c(2,5,8,10,11,115,-9)
result <-sort(v)
print(result)
reverse <- sort(v,decreasing = TRUE)
print(reverse)